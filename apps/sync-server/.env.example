# Must match core-api JWT_SECRET — the browser's ws token is signed by core-api
JWT_SECRET=supersecretchangeme

SANDBOX_RUNNER_URL=http://localhost:3004

# Not yet used (membership check deferred) — core-api base URL for future /membership calls
# CORE_API_URL=http://localhost:3002

PORT=3001
